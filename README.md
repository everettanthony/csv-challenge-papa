CSV Challenge with Papa Parse
========================================

Papa Parse is used to combine multiple CSV files into an HTML table. This example also adds a third column displaying the filename where the row's data came from.